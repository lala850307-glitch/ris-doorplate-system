# ── HTTP 爬蟲（直打 API，無需瀏覽器）────────────────────────
requests>=2.31.0
beautifulsoup4>=4.12.0

# ── 驗證碼辨識（ddddocr：專為中文網站驗證碼訓練的 ML 模型）─
ddddocr>=1.6.0
Pillow>=10.0.0

# ── 資料庫（PostgreSQL）──────────────────────────────────────
psycopg2-binary==2.9.9

# ── 排程器（APScheduler：支援時區、misfire 補跑）────────────
APScheduler==3.10.4

# ── 資料清洗 ─────────────────────────────────────────────────
pandas>=2.0.0

# ── 環境變數 ─────────────────────────────────────────────────
python-dotenv==1.0.1
